// Mirrors the `rigour.text.*` Python submodule layout — phonetic and
// character-level routines that feed into `rigour.text` end up here.

pub mod phonetics;
pub mod scripts;
pub mod transliterate;
