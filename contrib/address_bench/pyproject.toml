[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "address_bench"
version = "0.0.0"
readme = "README.md"
requires-python = ">= 3.12"
dependencies = [
    "anthropic",
    "click",
    "duckdb",
    "rich",
    "rigour",
]
